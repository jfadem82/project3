<p class="info">Every week in Los Angeles a fortune of money and energy is spent moving 12,000 tons of waste to Puente Hills landfill, the largest rubbish dump in America.<br> <br> On Average, 35,000 people move to Los Angeles each year from Asia, 15,000 from Central America, 8,000 from Europe, anda bout 8,500 from the New York Metro area, most of whom will want a couch, bed, dresser, microwave, etc. <br> <br>All items on this site are free and left as-is. Help save time and the planet by gaining or giving previously owned household items. <br>So come on...Show Me Your Junk!</p>
<ul class="index">
	<div class="row">
		<% for(var i=0; i < posts.length; i++) { %>
			<div class="col-sm-6 col-md-3 set-height">
				<li class="posting"><a href="/posts/<%= posts[i].id %>"><%= posts[i].title %></a></li>
	    	<li class="smaller2"><a href="/posts/<%= posts[i].id %>"><img src="<%= posts[i].avatar_url %>"></a></li>
	    	<li>
	    		<% if (user) {%>
	      		<% if (posts[i].user.toString() === user._id.toString()) { %>
			    		<a href="/posts/<%= posts[i].id %>/edit" class="glyphicon glyphicon-pencil"></a>
						<div class ="glyphicon glyphicon-trash nodemon delete" id="<%= posts[i].id %>"></div>
						<% } %>
	        <% } %>
				</li>
	    </div>
		<% } %>
	</div>
</ul>

<script type="text/javascript">
  $('.nodemon').click(callDelete)

  function callDelete(){
    console.log(this.id)

    $.ajax({
      url: '/posts/' + this.id,
      type: 'DELETE',
      dataType: 'json',
      success: function(result) {
        location.reload()
      }
    })
  };
</script>